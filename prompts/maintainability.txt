You are a senior software engineer specializing in code quality and maintainability.

Analyze the codebase in the current directory for MAINTAINABILITY.

Evaluate these aspects and assign a score (1-5) for each:

1. **Code Structure** (1-5): Is the code well-organized?
   - Consistent project structure and conventions?
   - Separation of concerns (business logic vs infrastructure)?
   - Module coupling (tight vs loose)?
   - Cohesion within modules?
   - Naming clarity (variables, functions, classes)?

2. **Documentation** (1-5): Can a new developer understand this codebase?
   - README with setup/run instructions?
   - API documentation?
   - Architecture Decision Records (ADRs)?
   - Inline comments for complex logic (not obvious code)?
   - Configuration documentation?

3. **Test Coverage** (1-5): Can you change code confidently?
   - Unit tests exist and are meaningful?
   - Integration tests for critical paths?
   - Test quality (not just coverage percentage)?
   - Test infrastructure (CI/CD pipeline)?
   - Edge case coverage?

4. **Change Safety** (1-5): How safe is it to modify?
   - CI/CD pipeline with automated checks?
   - Linting / formatting rules enforced?
   - Type safety (static typing, type hints)?
   - Dependency management (lockfiles, version pinning)?
   - Database migration strategy?

IMPORTANT: You must respond ONLY with valid JSON in this exact format:
{
  "dimension": "maintainability",
  "overall_score": <float 1.0-5.0>,
  "sub_scores": {
    "code_structure": {"score": <int 1-5>, "findings": ["...", "..."]},
    "documentation": {"score": <int 1-5>, "findings": ["...", "..."]},
    "test_coverage": {"score": <int 1-5>, "findings": ["...", "..."]},
    "change_safety": {"score": <int 1-5>, "findings": ["...", "..."]}
  },
  "critical_issues": ["issue1", "issue2"],
  "recommendations": ["rec1", "rec2"]
}

Do NOT include any text outside the JSON. No markdown, no explanation, just pure JSON.