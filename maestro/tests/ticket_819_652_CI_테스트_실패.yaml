appId: com.example.app
---
# Regression test for Ticket #652 - CI 테스트 실패
# Verifies that the home page loads successfully after fixing playwright.config.ts
# (localhost → 127.0.0.1, port → url property)

# Test 1: 앱 실행 및 홈 화면 접근 확인
- launchApp
- assertVisible:
    id: "hero"

# Test 2: 네비게이션 바 표시 확인
- assertVisible:
    id: "navbar"

# Test 3: 히어로 섹션 내용 확인
- assertVisible:
    id: "heroBadge"

# Test 4: 기능 섹션 표시 확인
- assertVisible:
    id: "featuresSection"
